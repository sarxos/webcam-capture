language: java
jdk:
#  - openjdk6
#  - oraclejdk7
#  - oraclejdk8
  - openjdk7

before_install:
  - mvn install -DskipTests=true -e -B

script:
  - mvn test -B

after_success:
  - mvn clean cobertura:cobertura coveralls:report -B -P ci

branches:
  except:
    - gh-pages

